apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-for-harness.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-for-harness.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-for-harness.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
